name: Product Service Local Workflow Reference

on:
  workflow_call:

jobs:
  documentation-only:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Active workflow is defined at repository root: .github/workflows/product-service-ci-cd.yml"

